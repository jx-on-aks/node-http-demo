apiVersion: v1
kind: Service
metadata:
  name: {{ template "node-http-demo.fullname" . }}
  labels:
    app: {{ template "node-http-demo.name" . }}
    chart: {{ template "node-http-demo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "node-http-demo.name" . }}
    release: {{ .Release.Name }}
